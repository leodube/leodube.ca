import { user, page, job, project, skill, image, seoTag } from "./models";

export const lists = {
  User: user,
  Page: page,
  Job: job,
  Project: project,
  Skill: skill,
  Image: image,
  SEO_Tag: seoTag,
};
